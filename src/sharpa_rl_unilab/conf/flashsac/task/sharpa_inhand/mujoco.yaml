# @package _global_
defaults:
  - /common/task
  - /common/protocol
  - _self_
algo:
  num_envs: ${hardware.num_envs}
  batch_size: 2048
  replay_buffer_n: 1280
  updates_per_step: 14
  learning_starts: 1
  policy_frequency: 2
  critic_hidden_dim: 512
training:
  env_steps_per_sync: 2
model:
  critic_normalization: false
